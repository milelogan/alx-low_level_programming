#include "main.h"
#define UNUSED(x) (void)(x)

/**
 * main - main function
 * @argc: argument count
 * @argv: array of arguments
 * Return: omo void
 */

int main(int argc, char *argv[])
{
	UNUSED(argc);
	printf("%s\n", argv[0]);
	return (0);
}
